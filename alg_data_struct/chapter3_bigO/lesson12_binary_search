def binary_search(target, arr):
    low = 0
    high = len(arr) - 1
    while low <= high:
        median = (low + high)//2  
        if arr[median] == target:
            return True
        elif arr[median] < target:
            low = median + 1
        else:
            high = median - 1
    return False
    """
    Note how here you actually require the while loop, because there is a condition attached to it and no for loop.
    We put the return statement outside of the while loop to say "after all has been scanned, return false if not true."
    The logic is just like before.
    """
